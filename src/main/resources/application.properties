server.port=${PORT:8080}

# ── Database Configuration ───────────────────────────────────────────────────
# Active Connection (Uncomment one)

# Option A: Neon Cloud PostgreSQL (Default)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-solitary-fire-atpnbi3p-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_jf7YMvBJI1LW}

# Option B: Local PostgreSQL (Uncomment to use)
# spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/pvr_prime_naturals}
# spring.datasource.username=${DB_USERNAME:postgres}
# spring.datasource.password=${DB_PASSWORD:postgres}

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ── JWT ───────────────────────────────────────────────────────────────────────
pvr.app.jwtSecret=${JWT_SECRET:default-jwt-secret-change-in-production-must-be-very-long}
pvr.app.jwtExpirationMs=86400000

# ── Mail ──────────────────────────────────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:pradeepperumalla544@gmail.com}
spring.mail.password=${MAIL_PASSWORD:jjwl ykom wsjm vizq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ── AWS S3 ────────────────────────────────────────────────────────────────────
aws.s3.access-key=${AWS_ACCESS_KEY:changeme}
aws.s3.secret-key=${AWS_SECRET_KEY:changeme}
aws.s3.bucket=${AWS_S3_BUCKET:ajapvr}
aws.s3.region=${AWS_S3_REGION:ap-south-1}

# ── Razorpay ──────────────────────────────────────────────────────────────────
# Original configuration
# razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_SbhMcQGUAQtBBx}
# razorpay.key.secret=${RAZORPAY_KEY_SECRET:changeme}

# Active Test Mode Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_T89iO66NMncEzs}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:HQa3mrgFqjAQ9lMg3iXv5yhl}

# ── CORS ─────────────────────────────────────────────────────────────────────
# CORS allowed origins – include both local dev and production URLs
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://pvr-prime-naturals-fe.vercel.app,http://localhost:5173}

# ── Actuator ──────────────────────────────────────────────────────────────────
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# ── Frontend & Cookie Security ────────────────────────────────────────────────
# Set to false for local HTTP dev; set to true + update url for production
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
pvr.app.cookie.secure=${COOKIE_SECURE:false}
